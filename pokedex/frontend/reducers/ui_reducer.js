const uiReducer = (state={}, action) => {
  Object.freeze(state);
  switch (action.type) {
    // case expression:
    //
    //   break;
    default:
      return state;
  }
};

export default uiReducer;
